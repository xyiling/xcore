---
title: 段描述符
date: 2025-03-29
keywords: [gdt, ldt]
---

## 段描述符

在操作系统中，段描述符（Segment Descriptor）是用来定义和管理内存段的一种数据结构，尤其是在使用分段内存管理模型的架构（如x86架构）中。段描述符存储了内存段的基址、段的大小、访问权限等信息，操作系统通过这些描述符来进行内存管理。
段描述符的组成：

一个典型的段描述符通常包括以下几个字段：

- 段基址（Base Address）：该段在物理内存中的起始地址，通常分为多个部分存储。比如在x86架构中，基址分为低16位、中16位和高32位。
- 段大小（Limit）：段的大小或者有效地址范围，指示该段的大小限制。也有分为16位和20位限制的情况。
- 访问权限（Access Rights）：指定该段的访问控制信息，如读、写、执行权限等。还可以定义段是否为可访问或是存在等属性。
- 段类型（Type）：标识该段的类型，通常有代码段、数据段、堆栈段等不同类型的段，操作系统根据类型来控制该段的使用。
- DPL（Descriptor Privilege Level）：描述符特权级，指示该段的访问级别。通常有4个级别，从0（最高特权）到3（最低特权）。操作系统通过该字段来管理权限和保护。
- 段存在位（Segment Present）：标记该段是否存在。若段不存在，访问该段会引发异常。
- G位和S位：G（Granularity）位指示段的粒度（1字节还是4KB），S（Descriptor Type）位指示段的类型（系统段还是应用段）。

段描述符在操作系统中的管理：

- 加载段描述符：操作系统通过段描述符表（GDT，Global Descriptor Table 或者 LDT，Local Descriptor Table）来维护段描述符。GDT是全局的，而LDT则是每个任务或进程的局部段描述符表。
- 访问控制：每次进程访问某个段时，操作系统会通过段描述符来检查访问权限。如果访问不允许，操作系统会抛出保护异常（如“段越界”或“权限不足”）。
- 段选择符（Segment Selector）：程序使用段选择符来引用特定的段描述符。段选择符包含一个指向段描述符表的索引，操作系统根据这个索引查找段描述符，然后根据段描述符中的信息来执行相关操作。
- 保护模式：在现代操作系统中，特别是使用x86架构时，操作系统会使用分段保护模式来提供内存保护。操作系统通过段描述符来实现对程序的隔离，防止一个程序访问到其他程序的内存区域。

## 设置段描述符
