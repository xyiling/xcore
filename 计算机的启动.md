对于8086芯片，除cs代码段寄存器外所有寄存器在通电后同意初始化为0
cs代码段寄存器则是0xffff
**第一条指令**位于0xffff0，即0xffff<<4 + 0000，前者是cs，后者是IP寄存器

8086可以访问1MB的内存，起始地址0x00000，结束地址0xfffff
内存在物理上不连续
```txt
0x00000~0x9ffff 内存区域 640kB
0xa0000~0xeffff 外部设备 320kB
0xf0000~0xfffff rom bios存储芯片 64kB 固化了开机时需要的指令
```


dram动态随机访问存储器
每个存储单元需要定时刷新内部的电荷，否则导致数据丢失
刷新期间无法访问内存（刷新时间很小）

**处理器从0xffff0开始执行，到0xfffff，一共只有16B，完全不够？到底这块地址有什么指令？**
这块地址有一条**跳转指令**，5B大小
```asm
jmp 0xf000:0xe05b ;0xfe05b bios内存区域，固化了开机诊断、初始化等逻辑
```

CPU在离开BIOS内存地址之前，最后一段代码会将主引导扇区代码**从硬盘0盘0柱1扇区**读到0x7c00处，然后跳转到那里继续执行

**除非发生跳转，否则处理器一定是从低地址向高地址执行**


**从实模式到保护模式的切换步骤**‌：

1. ‌**设置必要的实模式环境**‌：包括设置实模式下的堆栈等‌1。
2. ‌**初始化全局描述符表（GDT）、局部描述符表（LDT）及中断描述符表（IDT）等**‌：这些表用于在保护模式下管理内存和中断‌1。
3. ‌**保存实模式下的堆栈地址到某内存处**‌，以便切换回实模式后恢复，同时可以保存DS、ES、FS、GS等数据段寄存器的值‌1。
4. ‌**加载全局描述符表至全局描述符表寄存器（GDTR），如果未定义中断描述符表，则关中断，然后打开地址线A20**‌‌1。
5. ‌**修改CR0寄存器的PE位为1，切换到保护模式**‌：CR0寄存器的PE位用于控制保护模式的启用‌12。
6. ‌**使用段间跳转指令转到保护模式下的段**‌，如果有局部描述符表，则首先加载局部描述符表至局部描述符表寄存器（LDTR）‌1。
7. ‌**设置保护模式下的堆栈段SS及堆栈指针ESP**‌‌1。
8. ‌**设置DS、ES、FS、GS等段寄存器**‌，防止无意中使用到未设置的数据段‌1。
9. ‌**准备切换回实模式**‌：用于切换回实模式的段必须是16位段且其段描述符必须定义在GDT中，其段限制必须是0FFFFH‌1。
10. ‌**修改CR0寄存器的PE位为0切换回实模式**‌：恢复堆栈段至切换到保护模式之前的状态，如有必要也可恢复DS、ES等数据段‌1。
11. ‌**关闭地址线A20**‌：如中断为关闭状态，则打开中断‌1。

‌**实模式和保护模式的区别**‌：

- ‌**地址总线**‌：实模式使用20根地址线，寻址范围为1MB；保护模式使用32位地址总线，寻址范围为4GB‌3。
- ‌**寄存器**‌：实模式下寄存器为16位，保护模式下扩展到32位‌3。
- ‌**内存访问**‌：实模式下直接访问物理内存，保护模式下通过全局描述符表间接访问内存，增加了安全性‌3。