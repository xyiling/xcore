## nasm语法

## nasm规范
1. 伪指令

类似`_start:`这样的标签，不被cpu认可，只是相当于一个符号，用来标识一片代码区域

2. $和$$
- \$是当前行的地址
- \$\$是当前段的地址
- $ - $$是段偏移

如果不声明section，则所有的代码都在一个段，这个段的起始地址是0

3. section + vstart=

这个vstart可以规定这个段将放在虚拟地址空间的具体的地址

