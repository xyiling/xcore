---
title: 内存检测
---

ARDS adress range descriptor structure

调用前输入

|寄存器或状态位|参数用途|
|-|-|
|eax|调用的功能号，这里用到的是0xe820|
|ebx|内存信息需要按类型多次返回，由于每次执行都只返回一种类型内存的ards结构，所以要记录下一个待返回的内存ards，在下一次中断调用时通过这个值告诉bios应该返回哪个ards，这就是后续值的作用，第一次调用时值为零，ebx具体值不关注，只取决于bios的具体实现，每次中断返回后bios会更新这个值|
|es:di|ards缓冲区，bios将读取到的内容写入这个寄存器指向的内存，每次都以ards格式返回|
|ecx|ards结构大小，表示bios写入的字节数，调用者和bios支持的大小是20字节，以后也许会扩展这个结构|
|edx|固定签名为0x534d4150，即为字符串smap的ASCII码，bios将ards结构信息写入es:di后，用这个签名校验信息是否正确|

返回值

|reg|usage|
|-|-|
|CF|cf=0 => 调用出错，cf=1 => 未出错|
|eax|字符串smap的ASCII码0x534d4150|
|es:di|ards缓冲地址，同输入值一样，返回时被bios填充数据|
|ecx|bios写入到es:di时需要的字节数，最少写入20B|
|ebx|后续值：下一个ards的位置，中断返回后bios会更新这个值，找到下一个待返回的ards结构，不需要手动更改，ebx=0时代表没有后续内容|
